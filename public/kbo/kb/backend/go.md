# go

Чему равна переменная x после исполнения данного сегмента программы?

x := 0

terms := []int{1, 2, 3}
for _, term := range terms {
    if term%2 == 0 {
        x += term
    }
}

Чему равна переменная y после исполнения данного сегмента программы?

y := 1

factors := map[int]int{
    1: 2,
    2: 3,
    3: 1,
}

for _, val := range factors {
    y *= factors[val]
}

Что будет выведено в печать?

type A struct {}

func (a A) Foo() string {
    return "a"
}

type B struct {
    A
}

func (b B) Foo() string {
    return "b"
}

type C struct {
    A
}

func main() {
    b := B{}
    c := C{}

    fmt.Println(b.Foo(), c.Foo())
}

Отметьте условие, при котором компилятор будет считать, что тип реализует интерфейс.
Выберите синтаксически корректную реализацию интерфейса fmt.Stringer на типе type A struct{}.
Как в Go определяется область видимости и экспортируемость переменных? Отметьте верные утверждения.
Отметьте синтаксически корректную декларацию публичного метода в Go.
Выберите синтаксически корректную сигнатуру теста в Go.
Какое ограничение накладывается на тестовые файлы в Go?
Какая из стандартных Go-утилит используется для сборки бинарных пакетов?
